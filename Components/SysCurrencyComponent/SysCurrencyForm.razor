<!-- #region Form -->
	<RadzenTemplateForm TItem="SysCurrencyModel" Data="@row" Submit=@OnSubmit>
		<RadzenStack>
			<!-- #region Toolbar -->
			<RadzenRow Gap="8">
				<RadzenButton ButtonType="ButtonType.Submit" ButtonStyle="ButtonStyle.Primary" Text="Save" />
				@if (ID != null)
				{
					<RadzenButton ButtonStyle=@(row.IsActive == 1 ? ButtonStyle.Danger : ButtonStyle.Success) Text=@(row.IsActive ==
					1 ? "Non Active" : "Active") Click="@ChangeActive" />
				}
				<RadzenButton ButtonStyle="ButtonStyle.Danger" Text="Back" Click="@Back" />
			</RadzenRow>
			<!-- #endregion -->

			<RadzenStack>
				<RadzenRow>
					<!-- #region Code -->
					<FormFieldTextBox Label="Code" Name="Code" @bind-Value="@row.Code" Disabled=@(ID != null) Max="3" />
					<!-- #endregion -->

					<!-- #region Description -->
					<FormFieldTextArea Label="Description" Name="Description" @bind-Value="@row.Description" Max="50"
						Required="true" />
					<!-- #endregion -->

					<!-- #region Is Active -->
					<FormFieldSwitch Name="IsActive" Label="Active" @bind-Value="@row.IsActive" Disabled />
					<!-- #endregion -->
				</RadzenRow>

				<RadzenRow>
					<!-- #region OJKCode -->
					<FormFieldTextBox Label="OJK Code" Name="OJKCode" @bind-Value="@row.OJKCode" Disabled=@(ID != null)
						Max="10" />
					<!-- #endregion -->

					<!-- #region Is Base -->
					<FormFieldSwitch Name="IsBase" Label="Base" @bind-Value="@row.IsBase" />
					<!-- #endregion -->
				</RadzenRow>
			</RadzenStack>
		</RadzenStack>
	</RadzenTemplateForm>
	<!-- #endregion -->